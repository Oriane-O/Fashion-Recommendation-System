# Fashion-Recommendation-System
This project implements a fashion item recommendation system using deep learning. It extracts visual features from clothing images with a pre-trained VGG16 model. The system takes an input image and returns the most visually similar fashion items.
